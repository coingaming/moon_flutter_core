import 'package:flutter/material.dart';

class MoonCoreButton extends StatelessWidget {
  const MoonCoreButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
