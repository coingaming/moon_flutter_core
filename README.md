# Moon Design System headless UI core

## Getting Started

Moon Core ships unstyled components. Bring your own `MixScope`, tokens, and
styling when integrating the widgets into an application. The example app in
`example/` demonstrates one way to provide design tokens on top of the headless
primitives.
